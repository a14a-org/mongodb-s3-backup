{
  "name": "mongodb-s3-backup",
  "version": "1.0.0",
  "description": "Automated MongoDB backup to S3 with retention management",
  "main": "src/backup.js",
  "scripts": {
    "start": "node src/backup.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prettify": "prettier --write \"src/**/*.js\" \"config/**/*.js\""
  },
  "keywords": [
    "mongodb",
    "backup",
    "s3",
    "aws",
    "automation"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.511.0",
    "dotenv": "^16.4.1",
    "tar": "^6.2.0",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  },
  "engines": {
    "node": ">=22.11.0"
  }
} 